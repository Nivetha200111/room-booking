# Copy to .env.local and fill in your Supabase project values.
# Settings → API in the Supabase dashboard.
# If these are left empty the app runs in local-only mode (localStorage, no sharing).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
